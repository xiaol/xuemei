from rest_framework import serializers, pagination
from userena.contrib.umessages.models import MessageContact,Message
from userena.serializers import UserDetailSerializer

class MessageSerializer(serializers.ModelSerializer):
    class Meta:
	model = Message
        fields = ('body','send_at',)

class MessageContactSerializer(serializers.ModelSerializer):
    message_set = MessageSerializer
    from_user = UserDetailSerializer

    class Meta:
	model = MessageContact
     	fields = ('from_user','message_set',)
